# Copy this file to .env and fill in your key to enable the LLM-backed
# planner/checker. The agent runs fine without it -- it falls back to a
# deterministic keyword-based planner and skips the LLM semantic check.
ANTHROPIC_API_KEY=

# Optional: override the model used for planning/checking.
# CLAUDE_MODEL=claude-sonnet-5
